<% if walk.dogs.count > 0 %>
  <p>The following dogs are booked for this walk!</p>
  <% walk.dogs.each do |d| %>
      <div class="input-group" align="left" >
        <div class="form-inline">
          <p><%= d.name %>
          <% if d.user.id == current_user[:id] %>
            <%= form_for walk do |f| %>
              <%= hidden_field_tag(:dog_id, d.id) %>
              <%= text_field_tag("note", params[:note], :size => 15, :placeholder=> "") %>
              <%= button_to 'Add Note', {:action => "update"}, {:method => :patch } %>
            <% end %>
          <% end %>
      </div>
    </div></p>
  <% end %>
<% end %>
